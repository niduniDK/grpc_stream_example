openapi: 3.0.1
info:
  title: /
  version: 0.1.0
servers:
- url: "{server}:{port}/"
  variables:
    server:
      default: http://localhost
    port:
      default: "7040"
paths:
  /healthz:
    get:
      operationId: getHealthz
      responses:
        "200":
          description: Ok
          content:
            text/plain:
              schema:
                type: string
